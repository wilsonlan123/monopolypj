#include<iostream>
#include<list>
#include<vector>
#include<random>
#include<map>
#include<string>
#include <cstdlib>
#include <ctime>
using namespace std;

vector<int> scorelist;
vector<int> oplist;

struct playerdata {
	string name;
	string pawn;
	int BankBalance;
	int position;
	bool NPC;
};

int hmfloop(vector<int> w);



vector<int> drawnearn(int size;
